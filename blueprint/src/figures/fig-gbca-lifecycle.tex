% GBCA specification instance (PLTS.ABA.GBCA.Step). Automata style.
\begin{tikzpicture}[
    ->, >={Stealth[round]}, auto, semithick,
    every state/.style={draw, minimum size=1.3cm, align=center, font=\small},
    lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt}]
  \node[state, initial, initial text={}] (unb) at (0,0)    {unbound};
  \node[state] (bnd) at (3.8,0)   {bound\,$v$\\ grade\,$\bot$};
  \node[state] (ga)  at (8.4,2.2) {grade\\A-side};
  \node[state] (gc)  at (8.4,-2.2){grade\\C-side};
  \path
    (unb) edge node[lbl, above]{\texttt{bindSet}} (bnd)
    (bnd) edge node[lbl, sloped, above]{\texttt{retA}: latch A} (ga)
    (bnd) edge node[lbl, sloped, below]{\texttt{retC}: dissent} (gc)
    (unb) edge[loop above, looseness=6]
      node[lbl, above]{\texttt{call} / \texttt{callLoop}} (unb)
    (bnd) edge[loop above, looseness=6]
      node[lbl, above]{\texttt{retB}} (bnd)
    (ga)  edge[loop right, looseness=6] node[lbl, right]{\texttt{retA}} (ga)
    (gc)  edge[loop right, looseness=6] node[lbl, right]{\texttt{retC}} (gc);
\end{tikzpicture}
