% The per-process presentation (PLTS.ABA.GBCA.perProcInst, PLTS.ABA.perProcCore):
% n per-process automata, one hidden rendezvous, the composite equal to the monolith.
\begin{tikzpicture}[
    >={Stealth[round]}, semithick,
    proc/.style={draw, rounded corners, align=center, font=\footnotesize,
      minimum width=1.8cm, minimum height=1.1cm, inner sep=3pt},
    mono/.style={draw, rounded corners, align=center, font=\footnotesize,
      minimum width=4.4cm, minimum height=1.0cm, inner sep=3pt},
    lbl/.style={font=\scriptsize, fill=white, inner sep=1.5pt},
    ann/.style={font=\scriptsize, align=center, inner sep=1.5pt}]
  % hidden-alphabet frame
  \draw[dashed, rounded corners] (-1.2,-2.5) rectangle (11.5,2.8);
  \node[font=\footnotesize] at (1.35,2.45)
    {$\mathrm{relabel} \circ \mathrm{abstract}\,\mathrm{netLabels}$};
  % the per-process automata
  \node[proc] (p1) at (0.0,0.6) {$\mathrm{procInst}_1$};
  \node[proc] (pi) at (2.6,0.6) {$\mathrm{procInst}_i$\\ receiver};
  \node[proc] (pj) at (7.0,0.6) {$\mathrm{procInst}_j$\\ sender};
  \node[proc] (pn) at (9.9,0.6) {$\mathrm{procInst}_n$};
  \node[font=\footnotesize] at (1.3,0.6) {$\cdots$};
  \node[font=\footnotesize] at (8.45,0.6) {$\cdots$};
  % synchronised visible bus
  \draw[thick] (-0.9,1.7) -- (10.9,1.7);
  \foreach \x in {0.0,2.6,7.0,9.9} \draw[->] (\x,1.7) -- (\x,1.15);
  \node[ann] at (6.3,1.95)
    {$\mathrm{callG}$, $\mathrm{retG}$, $\mathrm{fail}$: every component steps};
  \draw[->] (10.9,1.7) -- (10.9,3.3) node[ann, above]{visible};
  % the rendezvous
  \draw[->] (pj.west) -- node[lbl, above]{$\mathrm{net}(i,j,m) \to \tau$} (pi.east);
  \node[ann] at (7.0,-0.9) {sender guard\\ $m \in \mathrm{out}_j$;\\ $j$ unchanged};
  \node[ann] at (2.6,-0.9)
    {receiver write\\ $\mathrm{inbox}_i(j) \cup \{m\}$};
  % the non-participants idle
  \draw[->] (p1.south west) to[out=-110, in=-70, looseness=1.6]
    node[lbl, below]{idle} (p1.south east);
  \draw[->] (pn.south west) to[out=-110, in=-70, looseness=1.6]
    node[lbl, below]{idle} (pn.south east);
  \node[ann] at (4.9,-2.1)
    {each component's own $\tau$ is interleaved: one moves, the rest hold};
  % the monolithic system
  \node[mono] (impl) at (4.9,-3.8) {monolithic instance\\ $\mathrm{implInst}(P,r)$, Algorithm~2};
  \draw[<->, dashed] (4.9,-2.5) -- node[lbl, right]{$\operatorname{ATD}$ equal, along $\mathrm{unpack}$} (impl.north);
\end{tikzpicture}
