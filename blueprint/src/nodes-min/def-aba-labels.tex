\begin{definition}[Shared alphabet]
  \label{def:aba-labels}
  \lean{PLTS.ABA.Lab}\lean{PLTS.ABA.Lab.hiddenAPI}\leanok
  \uses{def:aba-params}
  One label type carries the ABA interface, the round-tagged GBCA and WCC interfaces, the corruption broadcast and $\tau$, and the two sub-protocol interfaces make up the hidden alphabet of every composed system: \leandecl{PLTS.ABA.Lab} and \leandecl{PLTS.ABA.Lab.hiddenAPI}.
\end{definition}
