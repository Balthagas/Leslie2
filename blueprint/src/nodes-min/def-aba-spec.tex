\begin{definition}[The ABA specification, repaired]
  \label{def:aba-spec}
  \lean{PLTS.ABA.SpecStep}\lean{PLTS.ABA.spec}\leanok
  \uses{def:aba-labels}
  Transition System 1 is the repaired ABA specification, ten rules over the ABA interface on a state carrying the pending calls, the processes that have returned, the bound value, the decision value, the coin outcome, the corrupted set and the Validity provenance ghost (D1, D3, D13): \leandecl{PLTS.ABA.SpecStep} and \leandecl{PLTS.ABA.spec}.
\end{definition}
