\begin{definition}[The full-synchronisation product]
  \label{def:syncproduct}
  \lean{PLTS.System.syncProduct}\lean{PLTS.System.syncProduct_isLTS}\leanok
  \uses{def:idle-family}
  The full-synchronisation product steps every component of a finite family at once on a visible label and exactly one component on $\tau$, ownership of a label being carried by the components and not by the operator: \leandecl{PLTS.System.syncProduct}.
\end{definition}
