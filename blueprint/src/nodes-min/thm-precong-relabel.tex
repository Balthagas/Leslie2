\begin{theorem}[Precongruence for restriction]
  \label{thm:precong-relabel}
  \lean{PLTS.WeakScheduler.relabel}\lean{PLTS.ProbabilisticForwardSimulation.relabel}\leanok
  \uses{def:forwardsim, def:relabel, def:weakstep}
  A forward simulation over an extended alphabet survives restriction along the left embedding, along the same relation: \leandecl{PLTS.WeakScheduler.relabel}, \leandecl{PLTS.ProbabilisticForwardSimulation.relabel}.
\end{theorem}

\begin{proof}\leanok
  \uses{def:relabel, def:weakstep, def:weaktau}
  See the Lean proofs of \leandecl{PLTS.WeakScheduler.relabel} and \leandecl{PLTS.ProbabilisticForwardSimulation.relabel}.
\end{proof}
