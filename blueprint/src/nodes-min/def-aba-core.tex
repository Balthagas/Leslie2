\begin{definition}[The ABA core]
  \label{def:aba-core}
  \lean{PLTS.ABA.core}\leanok
  \uses{def:aba-labels, def:aba-core-vocab}
  The round loop regrouped as one PLTS over the whole system state, running the five-phase handshake cycle and gating the external return on an $n-f$ DECIDED quorum: \leandecl{PLTS.ABA.core}.
\end{definition}
