\begin{theorem}[Precongruence for abstraction]
  \label{thm:precong-abstract}
  \lean{PLTS.ProbabilisticForwardSimulation.abstract}\leanok
  \uses{def:forwardsim, def:process-algebra}
  A forward simulation survives abstraction of a label set on both sides, along the same relation: \leandecl{PLTS.ProbabilisticForwardSimulation.abstract}.
\end{theorem}

\begin{proof}\leanok
  \uses{def:process-algebra, def:weakstep}
  See the Lean proof of \leandecl{PLTS.ProbabilisticForwardSimulation.abstract}.
\end{proof}
