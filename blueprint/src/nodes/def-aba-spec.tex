\begin{definition}[The ABA specification, repaired]
  \label{def:aba-spec}
  \lean{PLTS.ABA.SpecStep}\lean{PLTS.ABA.spec}\leanok
  \uses{def:aba-labels}
  Transition System 1 of~\cite{LeslieBP}, the ABA specification $\mathrm{spec}(P)$: ten rules over the ABA interface, on a state carrying the pending call of each process, the bound value $bind$, the decision value $val$, the coin outcome $coin$ and the corrupted set $F$, with four deviations.
  Corruption is a total deterministic transform (D1), and the Validity provenance apparatus (D13) supplies a ghost map $input$ recording genuine $\mathrm{callABA}$ events, an $f{+}1$ $F$-blind support guard on the mixed-bind rule~4, a provenance guard on the re-propose rule~7 (D3), and a tenth rule $\mathrm{callByzFill}$ by which a corrupted process fills an empty call slot with no ghost record, the abstract image of the concrete adversary's hidden Byzantine $\mathrm{callG}$ drivers.
  Full rule table: \leandecl{PLTS.ABA.spec}.
\end{definition}
